<Page
    x:Class="Harness.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedTheme="Dark">

    <!-- Apotheosis mobile shell. Keep every x:Name and event: code-behind treats XAML as an ABI. -->

    <Page.Resources>
        <SolidColorBrush x:Key="PageBg"    Color="#FF080D11"/>
        <SolidColorBrush x:Key="Chrome"    Color="#FF10171D"/>
        <SolidColorBrush x:Key="Surface"   Color="#FF172129"/>
        <SolidColorBrush x:Key="SurfaceHi" Color="#FF1D2B34"/>
        <SolidColorBrush x:Key="Inset"     Color="#FF090F13"/>
        <SolidColorBrush x:Key="Sep"       Color="#FF263640"/>
        <SolidColorBrush x:Key="TxtHi"     Color="#FFF4F7F8"/>
        <SolidColorBrush x:Key="TxtLo"     Color="#FF91A2AD"/>
        <SolidColorBrush x:Key="Accent"    Color="#FF45D6C5"/>
        <SolidColorBrush x:Key="AccentDim" Color="#FF183D3B"/>
        <SolidColorBrush x:Key="Warm"      Color="#FFFFB454"/>
        <SolidColorBrush x:Key="Danger"    Color="#FFFF6B6B"/>
        <SolidColorBrush x:Key="Scrim"     Color="#D9080D11"/>

        <!-- 底栏图标键:大触摸目标,透明底 -->
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TxtHi}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Width" Value="52"/>
            <Setter Property="Height" Value="62"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>
        <!-- 小图标键(查找条/抽屉关闭等) -->
        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TxtHi}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="TabBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TxtLo}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>
        <!-- 动作面板:顶部快捷图标列 -->
        <Style x:Key="QuickBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TxtHi}"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0,13"/>
        </Style>
        <!-- 动作面板:整行菜单项 -->
        <Style x:Key="MenuRow" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TxtHi}"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="18,14"/>
        </Style>
        <!-- 设置页的整行按钮 -->
        <Style x:Key="SetRow" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="Foreground" Value="{StaticResource TxtHi}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,13"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>
        <!-- 深色输入框(地址栏/查找条)。默认 TextBox 模板在 Focused 态把 BorderElement 刷白、
             把 ContentElement 切到 Light 主题(文字变黑) → 聚焦时整条地址栏发白。这里的模板
             (基于 SDK 15063 默认模板)让 Normal/PointerOver/Focused 三态外观完全一致,
             并把内置 DeleteButton 的触摸目标放大到 44x40(字形仍是 14);触摸区只向左扩,
             ✕ 本身仍贴输入框右缘(与默认模板同位置)。
             注意:不要给模板加 VerticalContentAlignment 绑定 —— 文字垂直居中仍靠元素上的 Padding。 -->
        <Style x:Key="DarkFieldBox" TargetType="TextBox">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource TxtHi}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SelectionHighlightColor" Value="{StaticResource Accent}"/>
            <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Grid.Resources>
                                <!-- 清除键:透明底,大触摸目标,字形不变大 -->
                                <Style x:Key="DarkFieldDeleteButtonStyle" TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Grid x:Name="ButtonLayoutGrid" Background="Transparent" BorderThickness="0">
                                                    <VisualStateManager.VisualStateGroups>
                                                        <VisualStateGroup x:Name="CommonStates">
                                                            <VisualState x:Name="Normal"/>
                                                            <VisualState x:Name="PointerOver">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="GlyphElement" Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource TxtHi}"/>
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="Pressed">
                                                                <Storyboard>
                                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="GlyphElement" Storyboard.TargetProperty="Foreground">
                                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource Accent}"/>
                                                                    </ObjectAnimationUsingKeyFrames>
                                                                </Storyboard>
                                                            </VisualState>
                                                            <VisualState x:Name="Disabled">
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetName="ButtonLayoutGrid" Storyboard.TargetProperty="Opacity" To="0" Duration="0"/>
                                                                </Storyboard>
                                                            </VisualState>
                                                        </VisualStateGroup>
                                                    </VisualStateManager.VisualStateGroups>
                                                    <!-- 字形靠右钉死:按钮比默认模板的 34 宽了 10,居中会把 ✕ 往左推 10/2+…;
                                                         右边距 10 让字形正好落在默认模板的位置,多出来的宽度全部向左扩成触摸区。 -->
                                                    <TextBlock x:Name="GlyphElement" Text="&#xE10A;"
                                                               FontFamily="Segoe MDL2 Assets" FontSize="14" FontStyle="Normal"
                                                               Foreground="{StaticResource TxtLo}"
                                                               HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"
                                                               AutomationProperties.AccessibilityView="Raw"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Grid.Resources>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <!-- 空:PointerOver / Focused 与 Normal 完全同貌 -->
                                    <VisualState x:Name="PointerOver"/>
                                    <VisualState x:Name="Focused"/>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource TxtLo}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PlaceholderTextContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource TxtLo}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="ButtonStates">
                                    <VisualState x:Name="ButtonVisible">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="DeleteButton" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0">
                                                    <DiscreteObjectKeyFrame.Value>
                                                        <Visibility>Visible</Visibility>
                                                    </DiscreteObjectKeyFrame.Value>
                                                </DiscreteObjectKeyFrame>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="ButtonCollapsed"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border x:Name="BorderElement" Grid.Row="1" Grid.RowSpan="1" Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ContentPresenter x:Name="HeaderContentPresenter" x:DeferLoadStrategy="Lazy" Visibility="Collapsed"
                                              Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,8" FontWeight="Normal"
                                              Foreground="{StaticResource TxtLo}"
                                              Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              TextWrapping="{TemplateBinding TextWrapping}"/>
                            <ScrollViewer x:Name="ContentElement" Grid.Row="1"
                                          HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                          IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
                                          IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
                                          IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
                                          Margin="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}"
                                          IsTabStop="False" ZoomMode="Disabled"
                                          AutomationProperties.AccessibilityView="Raw"/>
                            <ContentPresenter x:Name="PlaceholderTextContentPresenter" Grid.Row="1" Grid.ColumnSpan="2"
                                              Foreground="{StaticResource TxtLo}" IsHitTestVisible="False"
                                              Margin="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}"
                                              Content="{TemplateBinding PlaceholderText}"
                                              TextWrapping="{TemplateBinding TextWrapping}"/>
                            <!-- 清除键固定贴输入框右缘(第 1 列 = Auto,靠右;绝不铺满内容列)。
                                 Margin 右 -2 = 默认模板的 HelperButtonThemePadding,位置与默认模板一致。 -->
                            <Button x:Name="DeleteButton" Grid.Row="1" Grid.Column="1"
                                    Style="{StaticResource DarkFieldDeleteButtonStyle}"
                                    Background="Transparent" BorderThickness="0" Padding="0" Margin="0,0,-2,0"
                                    MinWidth="44" MinHeight="36"
                                    HorizontalAlignment="Right" VerticalAlignment="Stretch"
                                    IsTabStop="False" Visibility="Collapsed"
                                    AutomationProperties.AccessibilityView="Raw"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="RootGrid" Background="{StaticResource PageBg}">
        <!-- 软键盘弹出时整体上移(仅地址栏聚焦时,见 code-behind InputPane 处理) -->
        <Grid.RenderTransform>
            <TranslateTransform x:Name="RootShift" Y="0"/>
        </Grid.RenderTransform>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>      <!-- 内容(网页) -->
            <RowDefinition Height="Auto"/>   <!-- 底部 chrome -->
        </Grid.RowDefinitions>

        <!-- ===== 内容区:网页渲染 + 浮层 ===== -->
        <Grid Grid.Row="0">
            <Border Background="White" Margin="6,6,6,0" CornerRadius="10">
                <!-- 自由滚动:内容区直接接 ManipulationDelta(单指拖→引擎滚动+惯性,捏合→Scale)。
                     点击走 Tapped;坐标用 GetPosition(ContentArea) 映回引擎像素(MapTapToEngine)。 -->
                <Grid x:Name="ContentArea" Background="White"
                       ManipulationMode="TranslateX,TranslateY,TranslateInertia,Scale"
                       IsTapEnabled="True" Tapped="OnPageTapped"
                       ManipulationDelta="OnImageManipDelta"
                       ManipulationCompleted="OnImageManipCompleted">
                    <!-- 固定 720x1080 的软件帧须随可用视口铺满；否则横屏时右侧会留下空白。 -->
                    <Image x:Name="RenderImage" Stretch="Fill"/>
                </Grid>
            </Border>

            <!-- GPU 直呈现面(SwapChainPanel);GPU 起来后承载 TextureMapper 合成输出。 -->
            <SwapChainPanel x:Name="GpuPanel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                            IsHitTestVisible="False" Visibility="Collapsed" Loaded="OnGpuPanelLoaded"/>

            <!-- 输入法捕获框:1×1 透明,聚焦唤起键盘;键入转发给引擎活会话。 -->
            <TextBox x:Name="ImeBox" Width="1" Height="1" Opacity="0" Margin="0" Padding="0"
                     BorderThickness="0" MinWidth="0" MinHeight="0"
                     HorizontalAlignment="Left" VerticalAlignment="Top" IsTabStop="True"
                     TextChanged="OnImeTextChanged" KeyDown="OnImeKeyDown"/>

            <!-- 顶部加载进度条 -->
            <ProgressBar x:Name="Progress" Height="3" VerticalAlignment="Top" Foreground="{StaticResource Accent}"
                         Background="Transparent" IsIndeterminate="False" Visibility="Collapsed"/>

            <!-- 悬浮翻页键(触发懒加载/看下方内容)。仅有会话时显示。 -->
            <StackPanel x:Name="ScrollFab" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                         Margin="0,0,16,18" Visibility="Collapsed">
                <Border Width="46" Height="46" CornerRadius="23" Background="{StaticResource SurfaceHi}" BorderBrush="{StaticResource Sep}" BorderThickness="1" Margin="0,0,0,9">
                    <Button Width="46" Height="46" Padding="0" Background="Transparent" Foreground="{StaticResource TxtHi}"
                             FontSize="19" BorderThickness="0" Content="&#x25B2;" Click="OnScrollUp"/>
                </Border>
                <Border Width="46" Height="46" CornerRadius="23" Background="{StaticResource Accent}">
                    <Button Width="46" Height="46" Padding="0" Background="Transparent" Foreground="#FF07110F"
                             FontSize="19" BorderThickness="0" Content="&#x25BC;" Click="OnScrollDown"/>
                </Border>
            </StackPanel>

            <!-- 页内查找条(顶部浮条) -->
            <Border x:Name="FindBar" VerticalAlignment="Top" HorizontalAlignment="Stretch"
                    Background="{StaticResource Chrome}" BorderBrush="{StaticResource Sep}" BorderThickness="0,0,0,1" Visibility="Collapsed">
                <Grid Margin="10,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="{StaticResource Inset}" BorderBrush="{StaticResource Sep}" BorderThickness="1" CornerRadius="18">
                        <TextBox x:Name="FindBox" Style="{StaticResource DarkFieldBox}" FontSize="15" Height="36" BorderThickness="0" Background="Transparent"
                                 Foreground="{StaticResource TxtHi}" VerticalContentAlignment="Center" MinHeight="0" Padding="10,8,10,6"
                                 PlaceholderText="页内查找" KeyDown="OnFindKeyDown" TextChanged="OnFindChanged"/>
                    </Border>
                    <TextBlock x:Name="FindCount" Grid.Column="1" Text="" Foreground="{StaticResource TxtLo}" FontSize="13" VerticalAlignment="Center" Margin="10,0"/>
                    <Button x:Name="FindPrev" Grid.Column="2" Style="{StaticResource IconBtn}" Content="&#x25B2;" FontSize="13" Click="OnFindPrev"/>
                    <Button x:Name="FindNext" Grid.Column="3" Style="{StaticResource IconBtn}" Content="&#x25BC;" FontSize="13" Click="OnFindNext"/>
                    <Button x:Name="FindClose" Grid.Column="4" Style="{StaticResource IconBtn}" Content="&#x2715;" Click="OnFindClose"/>
                </Grid>
            </Border>

            <!-- 地址栏建议下拉:锚定内容区底部 → 浮在底栏正上方。点项即导航。 -->
            <Border x:Name="SuggestPanel" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"
                    Background="{StaticResource Chrome}" BorderBrush="{StaticResource Sep}" BorderThickness="0,1,0,0" Margin="8,0" CornerRadius="12,12,0,0" Visibility="Collapsed">
                <ScrollViewer MaxHeight="340" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="SuggestList" Margin="8,6"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- ===== 底部 chrome:状态行 + 导航栏 ===== -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 细状态行:页面标题 / 临时提示(code 大量写 TitleText 当 toast) -->
            <Border Grid.Row="0" Background="{StaticResource Chrome}" BorderBrush="{StaticResource Sep}" BorderThickness="0,1,0,0">
                <Grid Margin="14,4,14,3">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Ellipse Grid.Column="0" Width="5" Height="5" Fill="{StaticResource Accent}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TitleText" Grid.Column="1" Text="APOTHEOSIS" Foreground="{StaticResource TxtLo}" FontSize="11"
                               CharacterSpacing="90" HorizontalAlignment="Left" TextTrimming="CharacterEllipsis"/>
                </Grid>
            </Border>

            <!-- 导航栏:[标签数] | 🔒 地址 [Go/⟳/✕] | ⋯ -->
            <Grid Grid.Row="1" Height="62" Background="{StaticResource Chrome}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 标签键:方框数字,点开标签切换器 -->
                <Button x:Name="TabsBtn" Grid.Column="0" Background="Transparent" BorderThickness="0" Width="54" Height="62" Padding="0" IsHoldingEnabled="False" Click="OnTabs">
                    <Border BorderBrush="{StaticResource Accent}" Background="{StaticResource AccentDim}" BorderThickness="1.5" CornerRadius="7" Width="28" Height="28">
                        <TextBlock x:Name="TabCountText" Text="1" Foreground="{StaticResource Accent}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Button>

                <Rectangle Grid.Column="1" Fill="{StaticResource Sep}" Width="1" Height="26" VerticalAlignment="Center"/>

                <!-- 地址胶囊:锁标 + 地址输入 + 上下文键(Go/刷新/停止)。三者统一 40 高。UWP TextBox 不理会 VerticalContentAlignment(模板没绑),文字靠 Padding 上下值居中。 -->
                <Border Grid.Column="2" Background="{StaticResource Inset}" BorderBrush="{StaticResource Sep}" BorderThickness="1" CornerRadius="20" Margin="7,9" Padding="0" Height="42">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!-- 锁标:固定居中槽,glyph 与地址文字同基线 -->
                        <TextBlock x:Name="LockIcon" Grid.Column="0" Text="" FontFamily="Segoe MDL2 Assets"
                                   FontSize="14" Foreground="{StaticResource Warm}" TextLineBounds="Tight"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0,2,0"/>
                        <TextBox x:Name="UrlBox" Grid.Column="1" Style="{StaticResource DarkFieldBox}" FontSize="15" Height="40" MinHeight="0" Margin="0" BorderThickness="0" Background="Transparent"
                                 Foreground="{StaticResource TxtHi}" VerticalAlignment="Center" VerticalContentAlignment="Center" Padding="6,10,6,8"
                                 InputScope="Url" Text="" PlaceholderText="搜索或输入网址"
                                 KeyDown="OnUrlKeyDown" TextChanged="OnUrlChanged"
                                 GotFocus="OnUrlGotFocus" LostFocus="OnUrlLostFocus"/>
                        <Button x:Name="UrlActionBtn" Grid.Column="2" Background="Transparent" BorderThickness="0"
                                Foreground="{StaticResource Accent}" Width="42" Height="40" Padding="0" FontSize="17"
                                VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                Content="&#x21BB;" Click="OnUrlAction"/>
                        <!-- 编辑地址时占用同一格:白色清除键顶掉刷新/停止键(见 OnUrlGotFocus/OnUrlLostFocus),
                             输入框因此拿到整条胶囊的宽度。IsTabStop=False → 点它不夺焦,软键盘不收。 -->
                        <Button x:Name="UrlClearBtn" Grid.Column="2" Background="Transparent" BorderThickness="0"
                                Foreground="{StaticResource TxtHi}" Width="44" Height="40" Padding="0" FontSize="17"
                                IsTabStop="False" Visibility="Collapsed"
                                VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                Content="&#x2715;" Click="OnUrlClear"/>
                    </Grid>
                </Border>

                <Rectangle Grid.Column="3" Fill="{StaticResource Sep}" Width="1" Height="26" VerticalAlignment="Center"/>

                <!-- 菜单(More):弹出底部动作面板 -->
                <Button x:Name="MenuBtn" Grid.Column="4" Style="{StaticResource NavBtn}" Content="&#xE712;" Click="OnMenu"/>
            </Grid>
        </Grid>

        <!-- ============================================================================
             以下为全屏/底部浮层。Grid.RowSpan=2 覆盖内容+底栏。
             ============================================================================ -->

        <!-- ===== 动作面板(⋯ 弹出的底部 sheet)===== -->
        <Grid x:Name="ActionMenu" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource Scrim}" Visibility="Collapsed" Tapped="OnActionScrimTap">
            <Border VerticalAlignment="Bottom" Background="{StaticResource Surface}" BorderBrush="{StaticResource Sep}" BorderThickness="1,1,1,0" CornerRadius="20,20,0,0" Margin="6,0" Tapped="OnSheetTap">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="520">
                    <StackPanel Margin="0,12,0,18">
                        <Grid Margin="18,0,18,10">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="PAGE COMMANDS" Foreground="{StaticResource Accent}" FontSize="11" CharacterSpacing="120"/>
                                <TextBlock Text="当前页面" Foreground="{StaticResource TxtHi}" FontSize="21" FontWeight="SemiBold" Margin="0,3,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="34" Height="4" CornerRadius="2" Background="{StaticResource Sep}" VerticalAlignment="Top" Margin="0,3,0,0"/>
                        </Grid>
                        <!-- 快捷:后退 / 前进 / 收藏。刷新已在地址栏上下文键提供，避免重复。 -->
                        <Grid Margin="10,2,10,8" Background="{StaticResource Inset}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="BackBtn" Grid.Column="0" Click="OnBack" Style="{StaticResource QuickBtn}" IsEnabled="False">
                                <StackPanel>
                                    <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="22" HorizontalAlignment="Center" Foreground="{StaticResource TxtHi}"/>
                                    <TextBlock Text="后退" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource TxtLo}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="FwdBtn" Grid.Column="1" Click="OnForward" Style="{StaticResource QuickBtn}" IsEnabled="False">
                                <StackPanel>
                                    <TextBlock Text="&#xE72A;" FontFamily="Segoe MDL2 Assets" FontSize="22" HorizontalAlignment="Center" Foreground="{StaticResource TxtHi}"/>
                                    <TextBlock Text="前进" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource TxtLo}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>
                            <Button Grid.Column="2" Tag="bookmark" Click="OnAction" Style="{StaticResource QuickBtn}">
                                <StackPanel>
                                    <TextBlock Text="&#xE734;" FontFamily="Segoe MDL2 Assets" FontSize="22" HorizontalAlignment="Center" Foreground="{StaticResource Warm}"/>
                                    <TextBlock x:Name="ActFavLabel" Text="收藏" FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource TxtLo}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Button>
                        </Grid>

                        <TextBlock Text="BROWSE" Foreground="{StaticResource TxtLo}" FontSize="10" CharacterSpacing="120" Margin="18,8,18,3"/>

                        <Button Tag="newtab" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="新标签页" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="home" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="主页" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="ua" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock x:Name="ActUaLabel" Text="桌面版网站" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="find" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="页内查找" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="share" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE72D;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="分享" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="copylink" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="复制链接" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="download" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE896;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="下载此页" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <TextBlock Text="LIBRARY" Foreground="{StaticResource TxtLo}" FontSize="10" CharacterSpacing="120" Margin="18,10,18,3"/>

                        <Button Tag="bookmarks" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE734;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="书签" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="history" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE81C;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="历史记录" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Tag="downloads" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="下载内容" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Border Height="1" Background="{StaticResource Sep}" Margin="16,10,16,5"/>

                        <Button Tag="settings" Click="OnAction" Style="{StaticResource MenuRow}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="17" Width="34" VerticalAlignment="Center" Foreground="{StaticResource TxtLo}"/>
                                <TextBlock Text="设置" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- ===== 抽屉:收藏/历史/下载 ===== -->
        <Grid x:Name="Drawer" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource PageBg}" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource Chrome}" Padding="8,8" BorderBrush="{StaticResource Sep}" BorderThickness="0,0,0,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="10,0" VerticalAlignment="Center">
                    <TextBlock Text="LIBRARY" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="140"/>
                    <TextBlock Text="浏览资料库" Foreground="{StaticResource TxtHi}" FontSize="21" FontWeight="SemiBold" Margin="0,2,0,0"/>
                </StackPanel>
                <Button x:Name="GpuBtn" Grid.Column="1" Style="{StaticResource TabBtn}" Foreground="{StaticResource TxtLo}"
                        Content="&#x1F5A5; GPU" Click="OnToggleGpu" VerticalAlignment="Center"/>
                <Button x:Name="UaBtn" Grid.Column="2" Style="{StaticResource TabBtn}" Foreground="{StaticResource Accent}"
                        Content="&#x1F4F1; 手机UA" Click="OnToggleUA" VerticalAlignment="Center"/>
                <Button Grid.Column="3" Style="{StaticResource IconBtn}" Content="&#x2715;" Click="OnDrawerClose"/>
            </Grid>

            <Grid Grid.Row="1" Background="{StaticResource Chrome}" Margin="8,8,8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="TabFav"  Grid.Column="0" Style="{StaticResource TabBtn}" Content="&#x2605; 收藏" Click="OnTabFav"/>
                <Button x:Name="TabHist" Grid.Column="1" Style="{StaticResource TabBtn}" Content="&#x1F551; 历史" Click="OnTabHist"/>
                <Button x:Name="TabDl"   Grid.Column="2" Style="{StaticResource TabBtn}" Content="&#x2193; 下载" Click="OnTabDl"/>
                <Button x:Name="ActionBtn" Grid.Column="3" Style="{StaticResource TabBtn}" Foreground="{StaticResource Accent}" Content="&#x2605; 收藏此页" Click="OnPrimaryAction"/>
            </Grid>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="DrawerList" Margin="10,10,10,18"/>
            </ScrollViewer>
        </Grid>

        <!-- ===== 设置页(全屏)===== -->
        <Grid x:Name="SettingsPage" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource PageBg}" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource Chrome}" Padding="8,8" BorderBrush="{StaticResource Sep}" BorderThickness="0,0,0,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Style="{StaticResource IconBtn}" FontFamily="Segoe MDL2 Assets" Content="&#xE72B;" Click="OnSettingsBack"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="7,0">
                    <TextBlock Text="SYSTEM" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="140"/>
                    <TextBlock Text="浏览器设置" Foreground="{StaticResource TxtHi}" FontSize="20" FontWeight="SemiBold" Margin="0,1,0,0"/>
                </StackPanel>
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="16,14">
                    <TextBlock Text="SEARCH &amp; START" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="130" Margin="0,6,0,7"/>
                    <TextBlock Text="默认搜索引擎" Foreground="{StaticResource TxtLo}" FontSize="13" Margin="0,0,0,4"/>
                    <ComboBox x:Name="SetSearchCombo" HorizontalAlignment="Stretch">
                        <ComboBoxItem Content="Bing"/>
                        <ComboBoxItem Content="Google"/>
                        <ComboBoxItem Content="DuckDuckGo"/>
                        <ComboBoxItem Content="百度"/>
                    </ComboBox>

                    <TextBlock Text="主页(URL,留空用内置主页)" Foreground="{StaticResource TxtLo}" FontSize="13" Margin="0,18,0,4"/>
                    <TextBox x:Name="SetHomeBox" HorizontalAlignment="Stretch" InputScope="Url" PlaceholderText="about:home"/>

                    <ToggleSwitch x:Name="SetUaSwitch" Header="启动请求桌面版网站" Foreground="{StaticResource TxtHi}" Margin="0,18,0,0"/>

                    <TextBlock Text="自定义 User-Agent(留空=用上面的开关;改后刷新网页生效)" Foreground="{StaticResource TxtLo}" FontSize="13" Margin="0,16,0,4"/>
                    <TextBox x:Name="SetUaCustomBox" HorizontalAlignment="Stretch" TextWrapping="Wrap" AcceptsReturn="False"
                             PlaceholderText="Mozilla/5.0 (Windows NT 10.0; Win64; x64) ... Chrome/120 Safari/537.36 Edg/120"/>

                    <Grid Margin="0,16,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="默认缩放" Foreground="{StaticResource TxtHi}" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" x:Name="SetZoomLabel" Text="100%" Foreground="{StaticResource TxtLo}" FontSize="14" VerticalAlignment="Center"/>
                    </Grid>
                    <Slider x:Name="SetZoomSlider" Minimum="50" Maximum="200" StepFrequency="10" Value="100" ValueChanged="OnZoomChanged"/>

                    <ToggleSwitch x:Name="SetTabModeSwitch" Header="并发多引擎标签(暂搁置,后续实现)" IsEnabled="False" Foreground="{StaticResource TxtHi}" Margin="0,4,0,0"/>

                    <TextBlock Text="RENDERING" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="130" Margin="0,22,0,7"/>
                    <ToggleSwitch x:Name="SetGpuSwitch" Header="默认启用 GPU 渲染(加载首个网页后自动开)" Foreground="{StaticResource TxtHi}" Margin="0,0,0,6"/>
                    <Button Tag="gpu" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Content="立即开启 GPU 合成(重启回软件)"/>

                    <TextBlock Text="PRIVACY" Foreground="{StaticResource Warm}" FontSize="10" CharacterSpacing="130" Margin="0,22,0,7"/>
                    <Button Tag="clearhist" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Content="清除历史记录" Margin="0,0,0,6"/>
                    <Button Tag="clearfav" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Content="清除全部收藏" Margin="0,0,0,6"/>
                    <Button Tag="cleardl" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Content="清除下载记录" Margin="0,0,0,6"/>
                    <Button Tag="clearcookies" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Foreground="{StaticResource Danger}" Content="清除 Cookie(退出全部登录)"/>

                    <TextBlock Text="DIAGNOSTICS" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="130" Margin="0,22,0,7"/>
                    <Button Tag="export" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Content="导出调试日志 / 崩溃 dump"/>

                    <!-- 开发者选项:默认关闭的调试用界面元素。 -->
                    <TextBlock Text="DEVELOPER" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="130" Margin="0,22,0,7"/>
                    <TextBlock Text="开发者选项" Foreground="{StaticResource TxtLo}" FontSize="13" Margin="0,0,0,4"/>
                    <ToggleSwitch x:Name="SetScrollFabSwitch" Header="显示翻页按钮" Foreground="{StaticResource TxtHi}" Margin="0,0,0,6"/>

                    <TextBlock Text="ABOUT" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="130" Margin="0,22,0,7"/>
                    <TextBlock x:Name="VersionText" Text="版本 —" Foreground="{StaticResource TxtHi}" FontSize="15" Margin="0,0,0,8"/>
                    <Button Tag="checkupdate" Click="OnSettingsBtn" Style="{StaticResource SetRow}" Content="检查更新(GitHub Releases)"/>

                    <TextBlock Text="EdgeHTML Reborn / Apotheosis — WebKit (WebCore) 2.52.4 — ARM32 UWP"
                               Foreground="#FF80868B" FontSize="12" TextWrapping="Wrap" Margin="0,22,0,24"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- ===== 标签切换器(全屏)===== -->
        <Grid x:Name="TabSwitcher" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource PageBg}" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource Chrome}" Padding="10,10" BorderBrush="{StaticResource Sep}" BorderThickness="0,0,0,1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="8,0" VerticalAlignment="Center">
                    <TextBlock Text="WORKSPACE" Foreground="{StaticResource Accent}" FontSize="10" CharacterSpacing="140"/>
                    <TextBlock x:Name="TabSwitcherTitle" Text="标签" Foreground="{StaticResource TxtHi}" FontSize="21" FontWeight="SemiBold" Margin="0,2,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource TabBtn}" Foreground="{StaticResource Accent}" Content="完成" Click="OnTabSwitcherDone"/>
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="TabList" Margin="12,12"/>
            </ScrollViewer>
            <Button Grid.Row="2" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                    Background="{StaticResource AccentDim}" Foreground="{StaticResource Accent}" BorderBrush="{StaticResource Accent}" BorderThickness="0,1,0,0" Padding="0,16" Click="OnNewTab">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="15" VerticalAlignment="Center" Foreground="{StaticResource Accent}"/>
                    <TextBlock Text="新建标签页" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource Accent}"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- ===== 首启 OOBE:欢迎 + 选语言(English / 中文)。仅全新安装(settings.ini 无 lang)弹出。 ===== -->
        <Grid x:Name="OobePanel" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource PageBg}" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="36,0" MaxWidth="380">
                <Border Width="64" Height="64" CornerRadius="32" BorderBrush="{StaticResource Accent}" BorderThickness="2" Background="{StaticResource AccentDim}" Margin="0,0,0,22">
                    <TextBlock Text="A" Foreground="{StaticResource Accent}" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="APOTHEOSIS" Foreground="{StaticResource Accent}" FontSize="12" CharacterSpacing="220" HorizontalAlignment="Center"/>
                <TextBlock Text="EdgeHTML Reborn" Foreground="{StaticResource TxtHi}" FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                <TextBlock Text="Modern web, reborn on Windows Phone" Foreground="{StaticResource TxtLo}" FontSize="13" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,10,0,0"/>
                <TextBlock Text="让被放弃的 Windows Phone 重新跑现代网页" Foreground="{StaticResource TxtLo}" FontSize="13" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Margin="0,2,0,0"/>
                <TextBlock Text="Choose your language · 选择语言" Foreground="{StaticResource TxtHi}" FontSize="16" HorizontalAlignment="Center" Margin="0,44,0,18"/>
                <Border Background="{StaticResource Accent}" CornerRadius="4" Margin="0,0,0,12">
                    <Button Tag="en" Click="OnOobeLang" Background="Transparent" Foreground="#FF07110F" BorderThickness="0"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Padding="0,16" FontSize="18" Content="English"/>
                </Border>
                <Border Background="{StaticResource Surface}" BorderBrush="{StaticResource Sep}" BorderThickness="1" CornerRadius="4">
                    <Button Tag="zh" Click="OnOobeLang" Background="Transparent" Foreground="{StaticResource TxtHi}" BorderThickness="0"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Center" Padding="0,16" FontSize="18" Content="中文"/>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
